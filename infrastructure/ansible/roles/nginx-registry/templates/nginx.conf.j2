server {
    listen 80;
    listen [::]:80;

    server_name registry.hq.appsecacademy.tech;
    
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
